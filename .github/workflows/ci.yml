name: MeshNet CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build_and_test:
    name: Build & Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}

      - name: Ensure gradlew is executable
        run: chmod +x gradlew

      - name: Run Detekt 
        run: ./gradlew detekt 

      - name: Run Unit Tests
        run: ./gradlew testDebugUnitTest 

      - name: Run Simulator Benchmarks
        run: ./gradlew :benchmark:runBenchmarks 
        
      - name: Assemble Debug APK
        run: ./gradlew assembleDebug

  build_docs:
    name: Build Documentation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      # - name: Setup Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'
          # Removed cache: 'pip' because it requires a requirements.txt or pyproject.toml
          # which does not exist in this Android repository.
          
      # - name: Install MkDocs Material
      #   run: |
      #     pip install mkdocs-material pymdown-extensions
          
      # - name: Build Docs
      #   run: mkdocs build --strict
